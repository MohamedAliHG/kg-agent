"""Placeholder ranking utilities for future retrieval strategies."""

from typing import Any


def preserve_order(results: list[dict[str, Any]]) -> list[dict[str, Any]]:
    """Return results in their existing order."""
    return results
